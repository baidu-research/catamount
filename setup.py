from setuptools import setup, find_packages

setup(
   name='cougr',
   version='0.9',
   description='CouGr: Compute Graph Analyzer',
   author='CouGr Developers',
   author_email='joel@baidu.com',
   packages=find_packages(),
   install_requires=[],
)
